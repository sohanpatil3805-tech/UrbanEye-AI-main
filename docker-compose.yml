version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example

  dashboard:
    build: ./dashboard
    ports:
      - "5173:80"
    depends_on:
      - backend
